import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.administracion.starter',
  appName: 'administracionUsuarios',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
